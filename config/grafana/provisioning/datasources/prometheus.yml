apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    uid: llm-gateway-prometheus
    jsonData:
      timeInterval: 15s
